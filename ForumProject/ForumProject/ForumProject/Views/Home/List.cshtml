@using ForumProject.Entities
@model  IEnumerable<MainCategoryByCities>
@{
    ViewBag.Title = "List";
}

@*<h2>Choose your City</h2>

@foreach (var item in @Model)
{
    
    <div>@Html.ActionLink(item.CityName, "Show_IntermediateCategory_List",new {controller="Home",action= "Show_IntermediateCategory_List",id=item.MainCategoryByCitiesId },new {@class="btn  btn-default btn-lg" })</div>
    <div>@item.MainCategoryByCitiesId</div>
}*@

<div class="container">
    <div class="row">
        <div class="col-lg-12"><h1 class="text-center well-lg" >Choose Your City</h1></div>
        <div class="col-lg-12"></div>
        <div class="col-lg-12"></div>
        <div class="col-lg-8 col-lg-offset-2">
@foreach (var item in @Model)
{

    <div class="col-lg-4 col-md-4 col-sm-4 form-group">@Html.ActionLink(item.CityName, "Show_IntermediateCategory_List", new { controller = "Home", action = "Show_IntermediateCategory_List", id = item.MainCategoryByCitiesId }, new { @class = "btn  btn-info btn-lg" })</div>
    
}

        </div>


    </div>
</div>
<link href="~/Content/Style.css" rel="stylesheet" />